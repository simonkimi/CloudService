version: '3'


services:
  redis:
    image: 'redis:alpine'

  web:
    build: .
    ports:
      - '8000:8000'
    links:
      - redis
    depends_on:
      - redis
    volumes:
      - '.:/var/service'
    command: 'gunicorn -b 0.0.0.0:8000 cloudService.wsgi'
    environment:
      DEBUG: '1'

  celery_beat:
    build: .
    links:
      - redis
    depends_on:
      - redis
    volumes_from:
      - web
    command: 'celery -A asynchronous beat'

  celery_worker:
    build: .
    links:
      - redis
    depends_on:
      - redis
    volumes_from:
      - web
    command: 'celery -A asynchronous worker -c 15 --loglevel=info'


